/* global describe, it */
'use strict'

// const Bugs = require('../index')

const chai = require('chai')
const assert = chai.assert

describe('Test Insert', () => {
  it('Insert', (done) => {
    assert.isTrue(true, 'Not Real')

    done()
  })
})
